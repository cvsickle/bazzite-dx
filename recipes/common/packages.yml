---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:
  - type: dnf
    repos:
      copr:
        - dejan/lazygit
        - imput/helium
      nonfree: rpmfusion
      cleanup: true
    install:
      packages:
        - neovim
        - lazygit
        - tree-sitter-cli
        - ripgrep
        - fd-find
        - helium-bin
